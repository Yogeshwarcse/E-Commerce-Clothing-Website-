# Clothing Backend

Install & run:

```
cd backend
npm install
cp .env.example .env
# edit .env
npm run dev
```

This is a minimal scaffold with auth, products, categories, cart, orders and Stripe checkout endpoints.
